remote_theme: jekyll/minima

title: Jan Verbesselt
description: Earth Observation expert at the interface of science, policy, and application.

# Minima v3 built-in dark mode:
# "auto"  = follows visitor's OS dark/light setting automatically (recommended)
# "dark"  = always dark
# "light" = always light (same as current)
# "solarized" / "solarized-dark" = alternative colour schemes
minima:
  skin: auto

author:
  name: Jan Verbesselt
  email: jan.verbesselt[@]belspo.be

markdown: kramdown
kramdown:
  input: GFM
plugins:
  - jekyll-feed
  - jekyll-remote-theme

url: "https://janverbesselt.github.io"
baseurl: ""

header_pages:
  - curriculum.md
  - resources.md
  - academic-teaching.md
  - insights.md
  - passions.md
  - contact.md

goatcounter_code: "janverbesselt"
